pullRequestPreviewsEnabled: true
pullRequestPreviewsExpireAfterDays: 4
services:
  - type: web
    name: questdb
    env: docker
    disk:
      name: questdb
      mountPath: /root/.questdb/db
      sizeGB: 20
